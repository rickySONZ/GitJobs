Reviews for  <%= link_to "#{@job.company} - #{@job.title} located in #{@job.location}", "/jobs/#{@job.id}" %><br><br>
<% @reviews.each do |review|%>
    Review by <%= review.user.username %> -
    <%= link_to review.title, "/reviews/#{review.id}" %> -
    <%= review.content %>

    <% end %>